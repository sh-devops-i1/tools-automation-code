format_version: 10
pipelines:
  vault:
    group: Tools
    materials:
      git-da613e5:
        git: https://github.com/sh-devops-i1/tools-automation-code.git
        shallow_clone: false
        auto_update: true
        branch: main
    stages:
      - Apply:
          fetch_materials: true
          approval:
            type: manual
          jobs:
            Ansible:
              tasks:
                - exec: {command: ansible-playbook, arguments: [ "-i", "vault-internal.mohdshuja.com,", "tools.yml", "-e", "tool_name=vault", "-e", "ansible_user=ec2-user", "-e", "ansible_password=DevOps321" ], run_if: passed}

